export enum TaskPriority {
  minor = 'minor',
  major = 'major',
  critical = 'critical',
  blocked = 'blocked',
  trivial = 'trivial',
}

export enum TaskStatus {
  open = 'open',
  toDo = 'to_do',
  inProgress = 'in_progress',
  done = 'done',
}
